# JWT_Authentication_RBAC
A simple Node.js and Express.js application that demonstrates authentication using JSON Web Tokens (JWT), role-based access control (RBAC), and token blacklisting for logout functionality.
